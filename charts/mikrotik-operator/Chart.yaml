apiVersion: v2
name: mikrotik-operator
description: Kubernetes operator for managing MikroTik RouterOS configuration
type: application
# Chart package version. Independent of operator image tags. Bump this whenever
# the packaged chart changes, including appVersion updates.
version: 0.2.0
# Default operator and admin UI image tag when image.tag / ui.image.tag are empty.
appVersion: v0.2.0
keywords:
- kubernetes
- operator
- mikrotik
- routeros
home: https://github.com/ZeljkoBenovic/mikrotik-operator
sources:
- https://github.com/ZeljkoBenovic/mikrotik-operator
